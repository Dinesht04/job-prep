var - variable, function block
let - variable, enclosing block
const - constant, enclosing block

expression, not hoisted only used after declatration => let a = () => {

}

func declaration moves function to the top of the scopeS

